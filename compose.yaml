services:
  ai-research-assistant:
    build:
      context: .
    container_name: ai-research-assistant
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
      - sqlite_data:/app/data
    restart: unless-stopped

volumes:
  sqlite_data: